test:
  override:
    - phpenv global 7.0
    - ./vendor/bin/phpunit --coverage-clover build/logs/clover.xml
    - phpenv global 7.1
    - ./vendor/bin/phpunit
    - phpenv global 7.2
    - ./vendor/bin/phpunit
  post:
    - php vendor/bin/coveralls -v
