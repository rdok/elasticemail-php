language: php

php:
  - 5.5.9
  - 5.6
  - 7.0
  - hhvm

matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm

before_install:
  - composer self-update
  - composer install --prefer-source --no-interaction

script:
  - composer install --prefer-source --no-interaction
